let nonrec f =
  \n#0 ->
    cond
      true# ->
        let nonrec n = n#0 in
        let nonrec res#1 = cons 1 (cons 2 null) in
        let nonrec x = head# res#1 in
        let nonrec y = head# (tail# res#1) in
        case n
          (\arg#2 ->
             cond
               > 1 arg#2 -> x
               true# -> y)