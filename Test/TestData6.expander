let nonrec f =
  \n ->
    let nonrec res#0 =
      check# (isList# (cons (\_ -> true#) (cons (\_ -> true#) null)))
        (cons 1 (cons 2 null)) in
    let nonrec x = head# res#0 in
    let nonrec y = head# (tail# res#0) in
    case n
      (\arg#1 ->
         cond
           > 1 arg#1 -> x
           true# -> y)