let nonrec lambda#0 = \_ -> true#
let nonrec lambda#1 = \_ -> true#
let nonrec lambda#2 =
  \arg#1 ->
    cond
      > 1 arg#1 -> x
      true# -> y
let nonrec f =
  \n ->
    let nonrec res#0 =
      check# (isList# (cons lambda#0 (cons lambda#1 null)))
        (cons 1 (cons 2 null)) in
    let nonrec x = head# res#0 in
    let nonrec y = head# (tail# res#0) in
    case n lambda#2