let nonrec lambda#4 = \_#1 -> true#
let nonrec lambda#5 = \_#2 -> true#
let nonrec lambda#6 =
  \arg#3 ->
    cond
      > 1 arg#3 -> x
      true# -> y
let nonrec f =
  \n ->
    let nonrec res#0 =
      check# (isList# (cons lambda#4 (cons lambda#5 null)))
        (cons 1 (cons 2 null)) in
    let nonrec x = head# res#0 in
    let nonrec y = head# (tail# res#0) in
    case n lambda#6