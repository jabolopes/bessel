let nonrec patChar =
  \arg#0 ->
    cond
      (\arg#1 ->
         cond
           isChar# arg#1 ->
             cond
               eqChar# 'a' arg#1 -> true#
               true# -> false#
           true# -> false#)
        arg#0 ->
        'a'
let nonrec patInt =
  \arg#2 ->
    cond
      (\arg#3 ->
         cond
           isInt# arg#3 ->
             cond
               eqInt# 0 arg#3 -> true#
               true# -> false#
           true# -> false#)
        arg#2 ->
        0
let nonrec patReal =
  \arg#4 ->
    cond
      (\arg#5 ->
         cond
           isReal# arg#5 ->
             cond
               eqReal# 0.0 arg#5 -> true#
               true# -> false#
           true# -> false#)
        arg#4 ->
        0.0
let nonrec patString =
  \arg#6 ->
    cond
      (\arg#7 ->
         cond
           isString# arg#7 ->
             cond
               eqString# "hello" arg#7 -> true#
               true# -> false#
           true# -> false#)
        arg#6 ->
        "hello"
let nonrec patVariantTag =
  \arg#8 ->
    cond
      isApple isInt arg#8 ->
        let nonrec x = unApple arg#8 in
        x
let nonrec patBinOp =
  \arg#9 ->
    cond
      isHeadTail#
        (\arg#10 ->
           cond
             isInt# arg#10 ->
               cond
                 eqInt# 0 arg#10 -> true#
                 true# -> false#
             true# -> false#)
        (isList#
           (cons
              (\arg#11 ->
                 cond
                   isInt# arg#11 ->
                     cond
                       eqInt# 1 arg#11 -> true#
                       true# -> false#
                   true# -> false#)
              null))
        arg#9 ->
        let nonrec x = head# arg#9 in
        let nonrec y = tail# arg#9 in
        cons 0 1
let nonrec patAll = \_ -> 0
let nonrec patBind = \x -> x
let nonrec patGuard = \arg#12 -> cond isInt arg#12 -> 0
let nonrec patBindGuard = \x -> cond isInt x -> x
let nonrec patEmptyList1 =
  \arg#13 -> cond isList# null arg#13 -> null
let nonrec patEmptyList2 = \x -> cond isList# null x -> x
let nonrec patList1 =
  \arg#14 ->
    cond
      isList#
        (cons (\_ -> true#)
           (cons isInt
              (cons isInt
                 (cons
                    (\arg#15 ->
                       cond
                         isInt# arg#15 ->
                           cond
                             eqInt# 1 arg#15 -> true#
                             true# -> false#
                         true# -> false#)
                    null))))
        arg#14 ->
        let nonrec x = head# arg#14 in
        let nonrec y = head# (tail# (tail# arg#14)) in
        cons 0 (cons 0 (cons 0 (cons 1 null)))
let nonrec patList2 =
  \arg#16 ->
    cond
      isList#
        (cons (\_ -> true#)
           (cons isInt
              (cons isInt
                 (cons
                    (\arg#17 ->
                       cond
                         isInt# arg#17 ->
                           cond
                             eqInt# 1 arg#17 -> true#
                             true# -> false#
                         true# -> false#)
                    null))))
        arg#16 ->
        let nonrec x = head# arg#16 in
        let nonrec y = head# (tail# (tail# arg#16)) in
        cons 0 (cons 0 (cons 0 (cons 1 null)))
let nonrec patEmptyTuple1 =
  \arg#18 -> cond isTuple0 arg#18 -> mkTuple0
let nonrec patEmptyTuple2 = \x -> cond isTuple0 x -> x
let nonrec patTuple1 =
  \arg#19 ->
    cond
      isTuple4
        (mkTuple4 (\_ -> true#) isInt isInt
           (\arg#20 ->
              cond
                isInt# arg#20 ->
                  cond
                    eqInt# 1 arg#20 -> true#
                    true# -> false#
                true# -> false#))
        arg#19 ->
        let nonrec x = tuple4Ref0# arg#19 in
        let nonrec y = tuple4Ref2# arg#19 in
        mkTuple4 0 0 0 1
let nonrec patTuple2 =
  \arg#21 ->
    cond
      isTuple4
        (mkTuple4 (\_ -> true#) isInt isInt
           (\arg#22 ->
              cond
                isInt# arg#22 ->
                  cond
                    eqInt# 1 arg#22 -> true#
                    true# -> false#
                true# -> false#))
        arg#21 ->
        let nonrec x = tuple4Ref0# arg#21 in
        let nonrec y = tuple4Ref2# arg#21 in
        mkTuple4 0 0 0 1