let nonrec lambda#32 =
  \arg#1 ->
    cond
      isChar# arg#1 ->
        cond
          eqChar# 'a' arg#1 -> true#
          true# -> false#
      true# -> false#
let nonrec res#0 = check# lambda#32 'a'
let nonrec lambda#33 =
  \arg#2 ->
    cond
      isChar# arg#2 ->
        cond
          eqChar# 'a' arg#2 -> true#
          true# -> false#
      true# -> false#
let nonrec patChar = check# lambda#33 'a'
let nonrec lambda#34 =
  \arg#4 ->
    cond
      isInt# arg#4 ->
        cond
          eqInt# 0 arg#4 -> true#
          true# -> false#
      true# -> false#
let nonrec res#3 = check# lambda#34 0
let nonrec lambda#35 =
  \arg#5 ->
    cond
      isInt# arg#5 ->
        cond
          eqInt# 0 arg#5 -> true#
          true# -> false#
      true# -> false#
let nonrec patInt = check# lambda#35 0
let nonrec lambda#36 =
  \arg#7 ->
    cond
      isReal# arg#7 ->
        cond
          eqReal# 0.0 arg#7 -> true#
          true# -> false#
      true# -> false#
let nonrec res#6 = check# lambda#36 0.0
let nonrec lambda#37 =
  \arg#8 ->
    cond
      isReal# arg#8 ->
        cond
          eqReal# 0.0 arg#8 -> true#
          true# -> false#
      true# -> false#
let nonrec patReal = check# lambda#37 0.0
let nonrec lambda#38 =
  \arg#10 ->
    cond
      isString# arg#10 ->
        cond
          eqString# "hello" arg#10 -> true#
          true# -> false#
      true# -> false#
let nonrec res#9 = check# lambda#38 "hello"
let nonrec lambda#39 =
  \arg#11 ->
    cond
      isString# arg#11 ->
        cond
          eqString# "hello" arg#11 -> true#
          true# -> false#
      true# -> false#
let nonrec patString = check# lambda#39 "hello"
let nonrec res#12 = check# (isApple isInt) mkApple0
let nonrec x = unApple res#12
let nonrec patVariantTag = check# (isApple isInt) (mkApple 0)
let nonrec x = unApple patVariantTag
let nonrec lambda#40 =
  \arg#14 ->
    cond
      isInt# arg#14 ->
        cond
          eqInt# 0 arg#14 -> true#
          true# -> false#
      true# -> false#
let nonrec lambda#41 =
  \arg#15 ->
    cond
      isInt# arg#15 ->
        cond
          eqInt# 1 arg#15 -> true#
          true# -> false#
      true# -> false#
let nonrec res#13 =
  check# (isHeadTail# lambda#40 (isList# (cons lambda#41 null)))
    (cons x y)
let nonrec x = head# res#13
let nonrec y = tail# res#13
let nonrec lambda#42 =
  \arg#16 ->
    cond
      isInt# arg#16 ->
        cond
          eqInt# 0 arg#16 -> true#
          true# -> false#
      true# -> false#
let nonrec lambda#43 =
  \arg#17 ->
    cond
      isInt# arg#17 ->
        cond
          eqInt# 1 arg#17 -> true#
          true# -> false#
      true# -> false#
let nonrec patBinOp =
  check# (isHeadTail# lambda#42 (isList# (cons lambda#43 null)))
    (cons 0 1)
let nonrec x = head# patBinOp
let nonrec y = tail# patBinOp
let nonrec _#18 = 0
let nonrec patAll = 0
let nonrec patBind = \x -> x
let nonrec res#19 = check# isInt 0
let nonrec patBindGuard = check# isInt 0
let nonrec res#20 = check# (isList# null) null
let nonrec patEmptyList = check# (isList# null) null
let nonrec lambda#44 = \_#23 -> true#
let nonrec lambda#45 =
  \arg#22 ->
    cond
      isInt# arg#22 ->
        cond
          eqInt# 1 arg#22 -> true#
          true# -> false#
      true# -> false#
let nonrec res#21 =
  check#
    (isList#
       (cons lambda#44 (cons isInt (cons isInt (cons lambda#45 null)))))
    (cons 0 (cons 0 (cons 0 (cons 1 null))))
let nonrec x = head# res#21
let nonrec y = head# (tail# (tail# res#21))
let nonrec lambda#46 = \_#25 -> true#
let nonrec lambda#47 =
  \arg#24 ->
    cond
      isInt# arg#24 ->
        cond
          eqInt# 1 arg#24 -> true#
          true# -> false#
      true# -> false#
let nonrec patList =
  check#
    (isList#
       (cons lambda#46 (cons isInt (cons isInt (cons lambda#47 null)))))
    (cons 0 (cons 0 (cons 0 (cons 1 null))))
let nonrec x = head# patList
let nonrec y = head# (tail# (tail# patList))
let nonrec res#26 = check# isTuple0 mkTuple0
let nonrec patEmptyTuple = check# isTuple0 x
let nonrec lambda#48 = \_#29 -> true#
let nonrec lambda#49 =
  \arg#28 ->
    cond
      isInt# arg#28 ->
        cond
          eqInt# 1 arg#28 -> true#
          true# -> false#
      true# -> false#
let nonrec res#27 =
  check# (isTuple4 (mkTuple4 lambda#48 isInt isInt lambda#49))
    (mkTuple4 0 0 0 1)
let nonrec x = tuple4Ref0# res#27
let nonrec y = tuple4Ref2# res#27
let nonrec lambda#50 = \_#31 -> true#
let nonrec lambda#51 =
  \arg#30 ->
    cond
      isInt# arg#30 ->
        cond
          eqInt# 1 arg#30 -> true#
          true# -> false#
      true# -> false#
let nonrec patTuple =
  check# (isTuple4 (mkTuple4 lambda#50 isInt isInt lambda#51))
    (mkTuple4 0 0 0 1)
let nonrec x = tuple4Ref0# patTuple
let nonrec y = tuple4Ref2# patTuple