let nonrec isFruit :: Fruit -> Bool = isType# "Fruit"
let nonrec isApple =
  \fn#0 ->
    \arg#1 -> cond isFruit arg#1 -> isVariant# "Fruit" 0 fn#0 arg#1
let nonrec isBanana =
  \fn#2 ->
    \arg#3 -> cond isFruit arg#3 -> isVariant# "Fruit" 1 fn#2 arg#3
let nonrec isFig =
  \fn#4 ->
    \arg#5 -> cond isFruit arg#5 -> isVariant# "Fruit" 2 fn#4 arg#5
let nonrec mkApple =
  (\r#6 -> cond isFruit r#6 -> r#6) (mkVariant# "Fruit" 0 mkTuple0)
let nonrec mkBanana =
  \x ->
    cond
      isInt x ->
        (\r#7 -> cond isFruit r#7 -> r#7) (mkVariant# "Fruit" 1 x)
let nonrec mkFig =
  \arg#8 ->
    cond
      isTuple2 (mkTuple2 isInt isReal) arg#8 ->
        (\r#9 -> cond isFruit r#9 -> r#9) (mkVariant# "Fruit" 2 arg#8)
let nonrec unApple =
  \arg#10 ->
    cond
      isFruit arg#10 ->
        (\r#11 -> cond isTuple0 r#11 -> r#11) (unVariant# arg#10)
let nonrec unBanana =
  \arg#12 ->
    cond
      isFruit arg#12 -> (\x -> cond isInt x -> x) (unVariant# arg#12)
let nonrec unFig =
  \arg#13 ->
    cond
      isFruit arg#13 ->
        (\r#14 -> cond isTuple2 (mkTuple2 isInt isReal) r#14 -> r#14)
          (unVariant# arg#13)
let nonrec f1 =
  \a ->
    cond
      isApple isTuple0 a -> 0
      isBanana isInt a ->
        let nonrec x = unBanana a in
        1
      isFig (isTuple2 (mkTuple2 isInt isReal)) a ->
        let nonrec x = tuple2Ref0# (unFig a) in
        let nonrec y = tuple2Ref1# (unFig a) in
        2